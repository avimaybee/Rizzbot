# Implementation Plans

Generated by the improve skill on 2026-08-11, scoped to the new Quick Mode
implementation (working tree at commit `0704ef7` + 5 uncommitted modified
files: `app/components/QuickModeScreen.tsx`, `app/components/HistoryScreen.tsx`,
`services/geminiService.ts`, `services/feedbackService.ts`, `types.ts`).

**Operational note for executors**: these plans are written against an
**uncommitted working tree** that contains the Quick Mode rework (5 modified
files listed above). Do NOT dispatch an executor into a fresh worktree checked
out from `0704ef7` — the rework would be absent and every "Current state"
excerpt would fail. Either (a) run the executor in a worktree/clone that
includes these uncommitted changes (e.g. `git add -A && git commit` first, on
the user's say-so), or (b) run on the main checkout. Every plan's drift check
assumes the 5 modified files above are present in the working tree.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Restore a working verification baseline | P0 | S–M | — | DONE |
| 002  | Make the copy button label match its payload | P1 | S | 001 | DONE |
| 003  | Extract pure quick-advice module + characterization tests | P1 | M | 001 | DONE |
| 004  | Explicit `degraded` flag instead of string heuristics | P1 | S | 003 | DONE |
| 005  | Fix amber text contrast (AA) in wait state + action pills | P2 | S | — | DONE |
| 006  | Remove nested interactive elements in tone pills | P3 | S | — | DONE |
| 007  | Share tone/action/copy logic between Quick and History | P2 | M | 003, 005 | DONE |
| 008  | Split QuickModeScreen into presentational components | P2 | M | 001, 003, 007 | DONE (deviation: screen 864 lines, not <800 — heuristic; pinned bar + wait advisory intentionally left in screen per plan) |
| 009  | Spike: stream Quick Mode replies into the hero card | P2 | L | — | DONE (report + gated prototype; runtime latency not measured — see 009-spike-report.md) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 unblocks everything: it deletes the dead shadcn scaffold that breaks
  `tsc --noEmit`, and adds `typecheck`/`test` scripts so every later plan has a
  machine-checkable gate.
- 003 extracts the pure normalize/fallback logic out of `geminiService.ts`
  (which imports Firebase and cannot run under `bun test`) and adds the
  characterization tests for the roast→bold merge. 004 and 007 build on its
  exported helpers.
- 005 must land before 007: the shared action-label map copies Quick Mode's
  color tokens, and 005 fixes the amber one.
- 008 (component split) is last among the refactors: it operates on code that
  is already de-duplicated (007) and covered by tests (003).
- 009 is a design/spike plan (investigation + prototype + report), independent
  of the others; it does not touch production code.

## Findings considered and rejected

- sessionStorage screenshot quota silent-drop (`app/utils/useSessionState.ts:21-23`):
  documented tradeoff ("drop silently, state still works in memory") — settled, not a finding.
- Fixed-position copy bar inside animated root: verified safe — the root
  `motion.div` transform settles before results mount.
- Swipe-to-change-variation gesture: matches the existing repo pattern
  (`PracticeScreen.tsx:1096`, `TherapistScreen.tsx:1354`).
- 506 kB bundle warning: pre-existing, out of the Quick Mode scope.
- `@grpc/grpc-js` high advisory (transitive via firebase): client-side SPA,
  low reachability; bump `firebase` opportunistically, not a plan.
